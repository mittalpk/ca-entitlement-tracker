# BK1-US-019 — Append-only audit trail row with 17 fields

**Workflow ID:** BK1
**Phase:** 3 — Core Build (Alpha)
**Status:** NOT STARTED
**Story points:** M

---

**As a** Compliance Officer (MiFID II Art. 25 reviewer),
**I want** every processed position to produce exactly one append-only audit-trail row with all 17 required fields, including `incidentFlag` and `breachNotes` for BREACH events,
**so that** the full processing history for any corporate-action event can be reconstructed from the log alone, without re-running the workflow.

**Traces to:** FR-023, FR-024, FR-025 (`requirements.md` §4.7, §6.6)

**Acceptance criteria:**
- Given any processed position, When the audit trail is checked, Then exactly one row exists with all 17 columns populated (nulls only where schema-defined as nullable).
- Given a BREACH-tier event, When the audit row is checked, Then `incidentFlag=TRUE` and `breachNotes` is populated with an LLM-drafted description of the missed deadline (TC-004).
- Given the audit-trail sheet, When any workflow run completes, Then no pre-existing row has been modified or deleted (append-only, FR-024).

**Definition of Ready:** Dispatch node built (BK1-US-018); audit-trail Sheet provisioned per `03-data-contract.md`/`requirements.md` §6.6 schema.

**Definition of Done:** Append node built; manual verification that a BREACH-tier run (TC-004 scenario) produces a correctly populated row without touching prior rows.

**Dependencies:** BK1-US-016, BK1-US-017, BK1-US-018
