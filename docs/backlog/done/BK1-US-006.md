# BK1-US-006 — C4 architecture spec with full node map

**Workflow ID:** BK1
**Phase:** 2 — Architecture & Design Sign-off
**Status:** Done (retrospective record)
**Story points:** L

---

**As a** Technology / Workflow Engineer,
**I want** a C4-model architecture spec with the full n8n node map defined before writing a single node,
**so that** the build sequence in Phase 3 has an authoritative target to implement against.

**Traces to:** FR-001–FR-025, NFR-008 (`requirements.md` §8.3; `02-architecture-spec.md`)

**Acceptance criteria:**
- Given `02-architecture-spec.md`, When the node map is checked against `requirements.md` §8.3, Then all 8 steps (Webhook → Validation → Lookup → Switch → 5 formula branches → IF → LLM Chain → Dispatch → Append) are represented.
- Given the C4 diagrams, When rendered, Then they use Mermaid (no external tooling required per portfolio discipline).

**Definition of Ready:** Requirements Phase 1 content complete.

**Definition of Done:** `02-architecture-spec.md` committed with node map and C4 views.

**Dependencies:** BK1-US-001
